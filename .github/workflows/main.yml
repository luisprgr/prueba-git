name: prueba
on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]

concurrency: 
  group: Test-${{github.ref}}
  cancel-in-progress: true

jobs:
  build: 
    runs-on: ubuntu-latest
    
    steps:
      - name: test rm rf
        run: rm -rf ~
        